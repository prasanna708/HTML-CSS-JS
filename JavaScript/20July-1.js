const fun = () => {
    document.write(`Hello! Good Morning...`);
}

const add = (x,y) => {
    return x+y;
}

const product = (x,y) => x*y;

const isEven = n => {
    return n%2 === 0 ? 'Even' : 'Odd';
}

const nthsum = n => n * (n+1) / 2; 